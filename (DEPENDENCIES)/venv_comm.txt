# Upgrade pip globally
python -m pip install --upgrade pip

# Install CUDA-enabled PyTorch (cu124)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# Install rest of dependencies globally
pip install numpy pandas matplotlib seaborn scikit-learn opencv-python opencv-contrib-python pillow tqdm scipy jupyterlab ipywidgets notebook scikit-image imageio moviepy plotly albumentations tensorboard tensorflow

# Virtual Env
python -m venv venv
venv\Scripts\activate

# CUDA check
python -c "import torch; print(torch.__version__); print('cuda:', torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"